requirement: "TEST_PRECOMP_ONLY_RESULT_PASS"
item: 1
tc: 1
id: 'TEST_PRECOMP_ONLY_RESULT_PASS_001'
description: 'Precomputed verification: only result_verification_pass field set'

general_initial_conditions:
  system:
    - "Shell environment is available"

initial_conditions:
  system:
    - "All commands can be executed"

test_sequences:
  - id: 1
    name: "Only Result Pass Sequence"
    description: |
                   Test sequence where only result_verification_pass is true.
                   Output verification field is not set (implicitly fails).
    initial_conditions:
      system:
        - "Shell is ready"
    steps:
      - step: 1
        description: "Step with only result verification passing"
        command: echo "Test 1"
        expected:
          success: true
          result: 0
          output: "Test 1"
        verification:
          result: "[[ $EXIT_CODE -eq 0 ]]"
          output: "grep -q 'Test 1' <<< \"$COMMAND_OUTPUT\""
      - step: 2
        description: "Another step with only result verification passing"
        command: echo "Test 2"
        expected:
          success: true
          result: 0
          output: "Test 2"
        verification:
          result: "[[ $EXIT_CODE -eq 0 ]]"
          output: "grep -q 'Test 2' <<< \"$COMMAND_OUTPUT\""
