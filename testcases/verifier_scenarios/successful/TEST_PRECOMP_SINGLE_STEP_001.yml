requirement: "TEST_PRECOMP_SINGLE_STEP"
item: 1
tc: 1
id: 'TEST_PRECOMP_SINGLE_STEP_001'
description: 'Precomputed verification: single step passing all verifications'

general_initial_conditions:
  system:
    - "Shell environment is available"

initial_conditions:
  system:
    - "All commands can be executed"

test_sequences:
  - id: 1
    name: "Single Step Sequence"
    description: |
                   Test sequence with only one step that passes all verifications.
    initial_conditions:
      system:
        - "Shell is ready"
    steps:
      - step: 1
        description: "Execute single command"
        command: echo "Single step test"
        expected:
          success: true
          result: 0
          output: "Single step test"
        verification:
          result: "[[ $EXIT_CODE -eq 0 ]]"
          output: "grep -q 'Single step test' <<< \"$COMMAND_OUTPUT\""
