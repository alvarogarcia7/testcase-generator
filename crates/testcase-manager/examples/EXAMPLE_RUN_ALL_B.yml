requirement: "EXAMPLE_RUN_ALL_TEST_B"
item: 1
tc: 2
id: 'EXAMPLE_RUN_ALL_B'
description: 'Second test case for run-all subcommand'

general_initial_conditions:
  system:
    - "Shell environment is available"

initial_conditions:
  system:
    - "All commands can be executed"

test_sequences:
  - id: 1
    name: "Simple Echo Test"
    description: |
                   Simple echo command test
    initial_conditions:
      system:
        - "Shell is ready"
    steps:
      - step: 1
        description: "Echo message B"
        command: echo "Test B"
        expected:
          success: true
          result: 0
          output: "Test B"
        verification:
          result: "[[ $EXIT_CODE -eq 0 ]]"
          output: "grep -q 'Test B' <<< \"$COMMAND_OUTPUT\""
