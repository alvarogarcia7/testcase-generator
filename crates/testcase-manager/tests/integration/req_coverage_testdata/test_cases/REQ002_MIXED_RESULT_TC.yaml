type: test_case
schema: tcms/test-case.schema.v1.json
requirement: REQ-002
item: 2
tc: 1
id: REQ002_MIXED_RESULT_TC
description: Additional test case for REQ-002 that passes (creating mixed results)
requirement_coverage:
  type: full
general_initial_conditions:
  system:
    - Shell environment is available
initial_conditions:
  system:
    - Commands can be executed
test_sequences:
  - id: 1
    name: Passing Test for REQ-002
    description: This test passes while another fails
    initial_conditions:
      system:
        - Shell is ready
    steps:
      - step: 1
        description: Successful command
        command: echo "REQ-002 success case"
        expected:
          success: true
          result: 0
          output: REQ-002 success case
        verification:
          result: '[[ $EXIT_CODE -eq 0 ]]'
          output: grep -q 'success case' <<< "$COMMAND_OUTPUT"
