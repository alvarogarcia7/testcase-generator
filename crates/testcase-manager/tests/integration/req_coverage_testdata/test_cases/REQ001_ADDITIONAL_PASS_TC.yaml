type: test_case
schema: tcms/test-case.schema.v1.json
requirement: REQ-001
item: 2
tc: 1
id: REQ001_ADDITIONAL_PASS_TC
description: Additional test case for REQ-001 that also passes
requirement_coverage:
  type: full
general_initial_conditions:
  system:
    - Shell environment is available
initial_conditions:
  system:
    - Commands can be executed
test_sequences:
  - id: 1
    name: Additional Verification
    description: Additional test for REQ-001
    initial_conditions:
      system:
        - Shell is ready
    steps:
      - step: 1
        description: Second test for REQ-001
        command: echo "REQ-001 additional validation"
        expected:
          success: true
          result: 0
          output: REQ-001 additional validation
        verification:
          result: '[[ $EXIT_CODE -eq 0 ]]'
          output: grep -q 'additional validation' <<< "$COMMAND_OUTPUT"
