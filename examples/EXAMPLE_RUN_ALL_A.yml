requirement: "EXAMPLE_RUN_ALL_TEST_A"
item: 1
tc: 1
id: 'EXAMPLE_RUN_ALL_A'
description: 'First test case for run-all subcommand'

general_initial_conditions:
  system:
    - "Shell environment is available"

initial_conditions:
  system:
    - "All commands can be executed"

test_sequences:
  - id: 1
    name: "Simple Echo Test"
    description: |
                   Simple echo command test
    initial_conditions:
      system:
        - "Shell is ready"
    steps:
      - step: 1
        description: "Echo message A"
        command: echo "Test A"
        expected:
          success: true
          result: 0
          output: "Test A"
        verification:
          result: "[[ $EXIT_CODE -eq 0 ]]"
          output: "grep -q 'Test A' <<< \"$COMMAND_OUTPUT\""
