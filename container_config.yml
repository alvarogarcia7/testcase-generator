# Default container configuration for verifier reports
# This file provides metadata for test execution reports generated by the verifier

# Report title
title: "Test Execution Results"

# Project name
project: "Test Case Manager - Verification Results"

# Optional: Environment information (e.g., "Development", "Staging", "Production")
environment: "Development"

# Optional: Platform information (e.g., "Linux x86_64", "macOS ARM64")
platform: "Linux x86_64"

# Optional: Executor information (e.g., "CI Pipeline v2.1", "Jenkins v3.2", "Local Development")
executor: "Local Development"
